{
  "name": "@pingidentity/mcp-agent-delegation",
  "version": "0.0.1",
  "private": true,
  "description": "MCP server demonstrating agent-to-agent delegation with RFC 8693 token exchange",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "mcp-agent-delegation": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "mcp",
    "delegation",
    "agent",
    "token-exchange",
    "rfc8693"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "@pingidentity/aic-mcp-sdk": "workspace:*",
    "dotenv": "^17.2.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

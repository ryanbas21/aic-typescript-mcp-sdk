# PingOne Advanced Identity Cloud (AIC) Configuration
# For agent-to-agent delegation demo

# Base URL of your PingOne AIC tenant
AM_URL=https://your-tenant.forgeblocks.com

# OAuth realm path
AM_REALM_PATH=/am/oauth2/realms/root/realms/alpha

# MCP Server's OAuth client (confidential client)
AM_CLIENT_ID=your-mcp-server-client-id
AM_CLIENT_SECRET=your-mcp-server-client-secret

# Downstream API audience (the service this MCP server calls on behalf of users)
DOWNSTREAM_API_AUDIENCE=https://api.example.com

# Optional: Scopes to request when exchanging tokens for downstream API
DOWNSTREAM_API_SCOPES=read write

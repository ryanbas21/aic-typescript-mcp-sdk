# PingOne Advanced Identity Cloud (AIC) Configuration
# These values configure the MCP server's OAuth client

# Base URL of your PingOne AIC tenant
AM_URL=https://your-tenant.forgeblocks.com

# OAuth client ID (must be a confidential client)
AM_CLIENT_ID=your-oauth-client-id

# OAuth client secret
AM_CLIENT_SECRET=your-oauth-client-secret

# OAuth realm path (default: /am/oauth2/realms/root/realms/alpha)
AM_REALM_PATH=/am/oauth2/realms/root/realms/alpha

# MCP Server Configuration
# Base URL where the MCP server is accessible (for OAuth callbacks)
MCP_SERVER_URL=http://127.0.0.1:3000

# OAuth redirect URI (default: {MCP_SERVER_URL}/oauth/callback)
# OAUTH_REDIRECT_URI=http://127.0.0.1:3000/oauth/callback
